# Evaluations

Offline measurement and comparison tools live here rather than under
`pipeline/`, which is reserved for production runtime code.

`chunk_eval/` contains the answer-span containment scorer used to assess
rechunking safety. Its contract tests remain under `tests/`.
